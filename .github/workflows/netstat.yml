name: mongo

on:
  push:
    branches: [ mongo ]

jobs:
  build:
    runs-on: ubuntu-18.04
    steps:
      - uses: actions/checkout@master
      - name: Install mongodb
        run: |
          apt-get update
          apt-get install -y mongodb
          
